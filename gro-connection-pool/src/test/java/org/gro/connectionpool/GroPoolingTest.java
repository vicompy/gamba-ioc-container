package org.gro.connectionpool;

import org.junit.Test;


public class GroPoolingTest {

	@Test
	public void test1() {




//		final Connection con = createMock(Connection.class);
////		then
//
//		final Driver driver = createMock(Driver.class);
//		thenReturn(con).when(driver).getConnection(anyString(), anyString(), anyString());

	}

}
